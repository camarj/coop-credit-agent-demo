# Postgres local (Docker Compose) — see docker-compose.yml
DATABASE_URL=postgres://coop:coop_local@localhost:5433/coop_credit_dev

# Anthropic SDK — slice 6 (policyAgent first LLM call)
# Console: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# OpenAI embeddings — slice 6 (text-embedding-3-small for RAG corpus)
# Console: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Langfuse — pendiente de slice 9
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=
# LANGFUSE_BASE_URL=https://cloud.langfuse.com

# Decision agent thresholds — slice 7
# CONFIDENCE_THRESHOLD=0.75
# TOKEN_BUDGET_PER_APPLICATION=50000
